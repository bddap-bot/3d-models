SCORE: 5/10

ISSUES:
1. `body` + `tray` — the mounted assembly leaves 2290 mm² of the 110 × 110 door clear straight through (full 110.5 mm width for z 0–17.5 mm, and only the 16 mm perch bar crossing up to z 43.5), an escape and head-entrapment hole in the cage wall; carry the front flange down to the sill across the full opening width, leaving only the tray slot, so the hopper closes the door the way a feeder cup does.
2. `tray` — retention is a 0.5 mm sawtooth working against 1.4 mm of free vertical play in the rail channel, so a conure can lift-and-tug the tray ~45 mm into the cage until the outlet overhangs the tray's rear wall and the whole litre dumps; replace it with a positive stop (full-height rib in the channel entering a matching notch, or cut the channel play below the tooth height).
3. `tray` — the chosen print pose (rot X 90) stands a 166 mm tower on a 3.9 mm wide, 320 mm² first layer tangent to the perch cylinder with the centre of mass at z 81.5, chosen on support volume alone; print rot X 180 instead (3190 mm² first layer, 47 mm tall) and accept 19.7 cm³ of support instead of 11.9.
4. `body` — 41.4 cm³ of support, 24% of the part, on the rail, shroud and hook undersides, against the "minimal supports" requirement; chamfer the rail and shroud undersides to 45° or steeper and taper the hook throats so the tray channel self-supports.
5. `tray` — the "5 mm rolled hull lip" is a half-cylinder cut through its own axis at z = 65, so the rim the bird bites is a flat band with square 90° edges at x = 44.0 and x = 50.75, contradicting the rounded-edges requirement; put the rim cylinder centre below the cut plane or hull the wall top to a sphere so the crown is genuinely round.
6. `hopper.scad` — the mount is not parametric: hooks are literal at x = ±30 with a 5.9 mm throat, so declaring `bar_gap` 10 / 11 / 13 silently yields 140.8 / 125.5 / 70.4 mm³ of hook-into-bar interference and `tray_wo=50` puts 1563 mm³ of tray inside the rails; derive hook X and rail X from `bar_gap` and `tray_wo` and add a bar-phase offset.
7. `body` + `tray` — the outlet plane sits 7 mm above the tray and side-shroud walls, so the pile that must choke the 88 × 80 slot is unconfined over its top 7 mm; rear containment ends at y = 96 against the 96.8 mm a 32° repose needs, a marginal continuous rear spill for the fine seed fraction; raise the tray walls and shroud to z = 72 or drop the slot plane to z = 65.
8. `body` — layer lines run across the 62° flow wall, a stated requirement that is unmet; the report's own defence is correct (flow-wall-down bbox 136 × 212 × 157, rejected as outside the print volume) but the fix is to reshape or split the bin, not to re-pose it.

VERIFIED:
- Regenerated from source, `openscad -D 'part="…"' -o … hopper.scad`: `body.stl`, `lid.stl`, `verify/cavity.stl` byte-identical (md5) to the shipped files; `tray.stl` differs in vertex order only (identical bbox and 118.97 cm³ volume). CGAL reports `Simple: yes` for all three.
- trimesh 4.9 (`critic/round-2/mesh.py`): body / tray / lid all watertight=True, winding consistent, `is_volume`=True, body_count=1; bboxes 136.000 × 153.458 × 164.200, 104.000 × 166.000 × 47.000, 103.000 × 147.955 × 23.200 — max single axis 166.0 vs 175 limit. Volumes 224.44 / 118.97 / 77.69 cm³.
- Capacity, independent of the shipped `cavity.stl` (`critic/round-2/cav.py`, boolean box − body): 997.13 cm³, cavity bbox 88.00 × 135.83 × 105.00, min (−44.0, 5.6, 72.0). Matches the claimed 997.1.
- Slot and wall angle (`critic/round-2/sec.py`): section at z = 72.01 is 88.0 × 80.01 mm (7040 mm²) vs the 50 mm requirement; face normals give 90.00 / 90.00 / 90.00 / 62.00° from horizontal; section area rises monotonically 7040 → 7414 → 9286 → 11948 mm² from z 72 to 176.9 with X fixed at 88.0 — a true wedge, no funnel.
- Wall thickness, 200 000 surface samples with inward ray casts (`critic/round-2/thick.py`): body 346/200000 under 2.4 mm, all clustered on the line y ≈ 86.1, z ≈ 72.3 (the r 1.2 outlet lip fillet); tray 1051/200000, every cluster centre at z = 65.00 (the square rim edge); lid 439/200000, all exactly 2.00 mm (the detent bead). No planar wall under 2.4 mm.
- Fit and interference, trimesh booleans against my own 12 mm-pitch bar wall with 4 mm rails and the 110 × 110 cut-out (`critic/round-2/fit.py`): body × wall, tray × wall, lid × wall = 0.000 mm³; body × tray, body × lid, tray × lid = 0.000 mm³.
- Door coverage, 221 × 221 ray grid along +Y through the opening (`critic/round-2/door.py`, map at `critic/round-2/img/door_openings.png`): 3486 mm² of 12100 mm² clear straight through; largest region 2290 mm² spanning x −55…55, z 0…43.5; clear width 110.5 mm at z ≤ 17.5, 87.5 mm at z = 70.
- Anti-dislodgement map (`critic/round-2/esc.py`): outward travel blocked at every u for lift t ≤ 21 (t = 21 still 5.0–32.5 mm³); first clear row t = 22 mm, not the 24 mm claimed.
- Tray sweep (`critic/round-2/fit.py`): free from t = −2 to +4; pull-out interference 1.54 mm³ at t = 6 rising to a constant 19.94 mm³ from t = 12 to 40 — a ≤0.5 mm ride-over against 1.4 mm of channel play.
- Discharge containment (`critic/round-2/spill.py`): 98.9 % of the 88 × 80 outlet footprint lands on the tray floor at z = 48, 1.1 % on the side shroud at z = 66, 0 % falls out — vertical fall is contained; free-board deficit 72 − 65 = 7 mm.
- PrusaSlicer 2.9.4, 0.4 nozzle / 0.2 layer / 3 perimeters / 15 % / 45° threshold, on pre-rotated meshes (`critic/round-2/slice.sh`): body rotX180 41.4 cm³ support (19 h 33 m), rotX0 43.3, rotY90 96.6; tray rotX90 11.9, rotX180 19.7; lid rotX180 1.4. Reproduces the report's table exactly. `body_flowdown` (rotX −62) bbox 136.0 × 212.0 × 156.7 → "All objects are outside of the print volume."
- First-layer footprint and overhang area (`critic/round-2/foot.py`, `poses.py`): tray_x90 320 mm² over 85.6 × 3.9 mm, CoM z 81.5, height 166; tray_x180 3190 mm², CoM z 18.9, height 47; body_x180 1634 mm² ring, CoM z 70.5; down-facing area steeper than 45° — body_x180 2773 mm², body_y90 32131 mm², tray_x90 4120 mm², lid_x180 51 mm².
- Parametricity probe (`critic/round-2/pert.py`): `chk_wb` with bar_gap 12 → empty; bar_gap 11 / 10 / 13 → 125.46 / 140.79 / 70.40 mm³ of body-into-bar interference. `chk_bt` with tray_wo 50 → 1563.34 mm³ of tray-into-rail interference. Changing `wall` to 2.6 or 4.0 leaves body × lid at 0.00 mm³.
- Renders I made myself, all in `/home/bot/scratch/hopper-critic/B2/critic/round-2/img/`: assembly, body, tray and lid each from front, back, left, top, iso and under (24 images), plus `asm_cut_side.png` / `asm_cut_iso.png` (x = 0 section) and `door_openings.png` (clear-area map).
