SCORE: 3/10

ISSUES:
1. `body` mount — the hopper cannot be installed: the material that must end up inside the cage spans z −10..120 (130 mm) through a 110 mm opening, while the 120 × 144 mm flange lying flat at y=0..3.5 forbids any tilt, so a 4-DOF collision-free search (θx/θy/θz + X/Y/Z against the barred wall) reaches only 2 of 42021 configurations from the seated pose and never reaches a fully-outside pose (needs Y=20.99); fix by making the lower retainer deployable — a rotating toggle or a separate wedge inserted from outside after the top claw hooks — so the inside-cage span during insertion is ≤110 mm.
2. `sidecover` — its `rimboss2d` slab occupies x 54.5..57, exactly where `body()`'s shell and rim ring already sit, giving 2.5 mm of interpenetration over the whole z 127.6..157.6 rim band (4.5 mm at the spigot, 1.71 cm³ total), so the cover cannot seat and the hopper cannot be closed; move the boss outboard to x 57..59.5 or subtract it from the body's +x shell.
3. `tray` — the drawer is a zero-clearance fit on four faces (outer walls y=9/86 vs pocket walls y=9/86, floor z=21 vs pocket floor z=21, rail top z=38 vs channel top z=38), so it will not slide once printed; apply the file's existing `clr`=0.4 to the tray outline, floor height and rail height (0.5 mm exists only at the rail tips).
4. `lid` — the body's and cover's square top rear corners (x=±60, y=120) stand 1.26 mm outside the lid's r=4.4 internal corner radius, giving 18.0 mm³ of interference at each rear corner in two z bands, so the lid bottoms out before its ridge reaches the groove; round the body/cover top outer corners to r≥4.5 or grow the lid outline's corner circles.
5. `tray` + `body` throat — there is no shut-off: withdrawing the drawer opens the 114 × 52 mm slot directly to atmosphere and dumps the ~1 L column onto the floor; add a full-width slide gate above the tray or give the tray a rear wall that wipes the slot closed as it withdraws.
6. `body` flange window — the window (z 48..92) does not overlap the tray (z 21..48, hidden behind a solid flange), so the bird must reach 31 mm above the perch top, over a 12.5 mm ledge, then blind 24 mm down a 22 mm-wide slot to the seed; drop the window to the tray rim (win_z0 ≈ 24) or raise the tray so its rim is the sill.
7. `body` flange window + `lid` — bird safety: a 100 × 44 mm hexagonal aperture opens into a closed 27 mm-deep pocket outside the cage that a green-cheek can push its head and shoulders into, and the lid's front face and skirt underside sit at y=0 in the bar plane at z 143.6..161.2 where a beak reaches through the 8.5 mm bar gaps to hook it; add a central rib to leave ≤45 mm clear width and set the lid and the upper front wall back to y ≥ +8 (or run the flange up to z=161).
8. `REPORT.md` §2 — the claim that the body's sub-2.4 mm samples "sit on the 45-degree end tapers of the mounting claws and perch and on the same kind of chamfer" is wrong for 40 of them, which my probe localises to x 43..47 in the z 130..170 band, i.e. the `cover_pockets` cut into the rim, plus further ones at the tray-aperture edges; re-attribute them or add a local boss.

VERIFIED:
- `openscad -D 'part="<p>"' -o …` for all six targets, then `diff <(sort a.stl) <(sort b.stl)` → 0 line differences for body/lid/tray: the shipped STLs are reproducible from `hopper.scad` (facet order only differs).
- trimesh, after `merge_vertices()`: body/sidecover/lid/tray/cavity all `watertight=True`, `winding=True`, `body_count=1`; volumes 323.27 / 41.22 / 76.99 / 52.54 / 1000.22 cm³. Bounding boxes 120×140.99×167.61, 16×120×109.61, 128×124×17.60, 122.40×82×23.50 mm — max 167.61 < 175. (Report's `watertight=True` is only true after merging; raw files are 2974/356/444/700 duplicate-vertex shells.)
- Capacity: `cavity.stl` volume = 1.0002 L; sections give a constant 114.00 mm × 52.00 mm outlet at z=48.2/50/55/60/65/69.5 (2.08 × a 25 mm slice, ≥50 mm), widening above z=70; all sloped cavity faces exactly 65.00° from horizontal over 16760 mm², vertical 27332 mm², horizontal 18939 mm².
- Door fit (static): only the body enters the −3.5<y<0 slab, at x ±45.24, z 0.84..108.16 — 0 vertices outside the 110×110 opening; material at y≤−3.5 spans x ±44.47, z −10.00..120.00, zspan 130.00 mm, ymin −20.99.
- Installability: own `kin.py`, 21483 surface points, θx grid −40..40° at 0.5°, Y grid −40..90 at 0.5 mm, free X/Z intervals derived exactly per configuration → 20232 free nodes, 17629 fully-outside, reachable from the seated pose = 2. Coarse 3-angle run (θx 2°, θy/θz ±20°, 2009 rotations, 524349 nodes) — same result.
- Interference by OpenSCAD `intersection()` + trimesh volume: body∩sidecover 1711.16 mm³ (lumps at x 44..60 / y 0..9.9 / z 127.61..157.61 = 739.97 mm³ and x 44..57 / y 110.1..120 = 668.79 mm³, plus 2 × 151.20 mm³ at the spigot); body∩lid 18.00 mm³ at x −60..−57.82, y 117.82..120; sidecover∩lid 18.00 mm³ mirrored; body∩tray 52.47 mm³ in 42 coincident-face slivers; sidecover∩tray empty.
- Ray probes: at y=1.5, z=140 the body spans x −60..57 and the cover x 54.5..60 (2.5 mm overlap); at y=4.0, z=140 the cover starts at x=52.5 (4.5 mm). Point containment at (55.5, 1.5, 140), (55.5, 4.0, 140) and (54.0, 4.5, 140) returns True for both body and sidecover. Along +Y at x=0: body pocket walls at y=9/86 and tray walls at y=9/86 (z=23, 42); along +Z at y=30: pocket floor top z=21, tray floor bottom z=21; at y=7.5: channel z 32..38, rail top z=38.
- Wall thickness, opposed-ray probe (dot<−0.7), ~58–60 k accepted samples/part: body min 0.00, p1 3.00, median 5.50, 220 samples (0.375 %) <2.4 mm, clustered at z −20..0 (claw tapers), z 20..48 and 48..100 (tray aperture/rails) and z 130..170 at x 43..47 (cover-snap pockets); sidecover min 2.50, lid min 3.60, tray min 0.21 (30 samples, all in z 32.2..34.1 = the rail chamfer).
- Overhangs in the stated orientations (faces >45° from vertical, bed face excluded): body 748.1 mm² (0.52 %), sidecover 485.6, lid 407.9, tray 0.0; bed contact 10756/9321/15844/8827 mm². Reproduces the report. I did not re-run PrusaSlicer.
- Renders: 30 `c-*.png` and 16 `v-*.png` (5–6 viewpoints per part and assembly), 5 `w-assembly-*.png` + 2 `w-body-*.png` at working distance, and `my-section-x0.png` (own mid-plane cut of body+cover+lid+tray), all in `/home/bot/scratch/hopper-critic/C1/critic/round-1/`; scripts `ck.py`, `ck2.py`, `ck3.py`, `ck4.py`, `ck5.py`, `kin.py`, `int_*.scad`, `mysec.scad`.
