{
  "rules": [
    {
      "paths": [
        "batarang/**"
      ],
      "tests": [
        "./build",
        "nix-shell -p 'python3.withPackages(ps: with ps; [trimesh numpy scipy shapely networkx])' --run 'python3 batarang/verify.py'",
        "bash -n batarang/render.sh batarang/slice-a1-mini.sh"
      ],
      "why": "verify the batarang solid, symmetry, first-layer area and packaged A1 mini slicing settings"
    },
    {
      "paths": [
        "avatar/reasonable-poly/**"
      ],
      "tests": [
        "./avatar/reasonable-poly/build.sh"
      ],
      "why": "the reasonable-poly avatar build regenerates Blender, VRM, GLB, texture and proof artifacts, then validates runtime motion and metadata"
    },
    {
      "paths": [
        "avatar/low-poly/**"
      ],
      "tests": [
        "./avatar/low-poly/build.sh"
      ],
      "why": "the avatar build regenerates Blender, VRM, GLB and proof artifacts, then validates runtime motion and metadata"
    },
    {
      "paths": [
        "hopper/v3/**"
      ],
      "tests": [
        "./build",
        "nix-shell -p openscad 'python3.withPackages(ps: with ps; [trimesh numpy rtree networkx scipy])' --run 'python3 hopper/v3/verify.py'",
        "nix-shell -p prusa-slicer --run 'bash hopper/v3/slice.sh'"
      ],
      "why": "hopper v3 adds generated fit, capacity, wall, and flow checks beyond the repository artifact build"
    },
    {
      "paths": [
        "**"
      ],
      "tests": [
        "./build"
      ],
      "why": "the repository build validates every model manifest and its generated artifacts"
    }
  ]
}
